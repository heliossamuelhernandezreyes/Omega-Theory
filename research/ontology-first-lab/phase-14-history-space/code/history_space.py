"""Omega Theory Phase 14 — history-space lift.

A finite history is a compatible path gamma=(x0,...,xn).
An ontological update extends it by one compatible state:
gamma -> gamma+y iff xn -> y in Gamma.

Because every extension strictly increases path length, Hist(Gamma) is graded
and has no nonempty directed cycles even if Gamma itself contains cycles.
"""
