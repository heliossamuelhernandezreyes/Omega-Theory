# Manifest — Fase 34

Estado: investigación nueva / no canónica.

Objetivo: separar distancia de endpoints, longitud histórica y retrabajo; estudiar invariancia bajo agrupación de pasos.

Resultados principales:
- `S_path = d_H + 2 N_cancel` para toggles binarios;
- no-go para costo no negativo, aditivo, reversible e invariante bajo agrupación arbitraria si la compresión conserva sólo endpoints;
- generadores ponderados por cardinalidad recuperan distancia mínima `d_H` independiente de la granularidad;
- distancia neta, actividad acumulada y retrabajo son observables distintos.

Archivos: `REPORT.md`, `results/summary.tsv`.
