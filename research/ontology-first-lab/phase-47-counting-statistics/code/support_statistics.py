"""Omega Theory Phase 47.
Exact support distribution for a set-valued macro-relation:
G_{b,c}(x)=((1+x)^c-1)^b. Derives counting-uniform mean and variance,
without interpreting counting measure as physical probability.
"""
