"""Omega Theory Phase 49.

Result summary:
- symmetry + source-block factorization implies Q(s)=prod_i a_c(|S_i|);
- this leaves c-1 normalized ratios for fixed target size c;
- Gibbs exp(-beta W) follows only after the stronger iid edge-factorization assumption.

The full numerical tables were generated from exact finite products over support profiles.
"""
