"""Omega Theory Phase 66.

Exact Moore minimization of the interaction automaton for n_A=1..4.
The exact result is d_* = 0,0,1,2. In particular n_A=4 falsifies universal
first-order closure found at n_A=3.

The full experiment enumerates all internal digraphs, all cross masks x->B,
uses colored Pi_* as output, and cross-edge toggles as inputs.
"""
