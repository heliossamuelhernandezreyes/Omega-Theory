# Cierre de Fase 70

**Estado:** cerrada.

**Resultado principal:** al cocientar las intervenciones por órbitas de aristas bajo `Aut(G)`, las clases predictivas dejan de coincidir con todos los microestados etiquetados y pasan a coincidir exactamente, en las cinco familias ejecutadas, con las órbitas de microestados bajo `Aut(G)`.

**Profundidad relacional:** `d_*^rel=1` para ciclo, 2D, árbol y completo; `d_*^rel=2` para el cubo 3D.

**Veredicto:** existe soporte estructural de que el etiquetado absoluto de Fase 69 sobreidentificaba estados y ocultaba profundidad relacional. Sin embargo la selección geométrica sigue `NO DERIVADA / SUBDETERMINADA` porque varias geometrías incompatibles comparten `d_*^rel=1`.

**D=3:** no derivada.

**Caso 4D:** no ejecutado exhaustivamente (`2^32` microestados); no se sustituyó por muestreo.

**Nuevo problema abierto:** determinar si `predictive quotient = quotient by Aut(G)` es un teorema bajo estas reglas o sólo una regularidad del dominio pequeño.

**Siguiente fase autorizada:** Fase 71.
